# Monitoring and logging dependencies
wandb>=0.16.0
psutil>=5.9.0
nvidia-ml-py3>=7.352.0  # Optional - for GPU monitoring

# Network resilience and caching
# (Uses built-in threading, queue, and time modules for RAM caching)