================================================================================
TRAINING STABILITY ISSUES - ROOT CAUSE ANALYSIS & FIX PLAN
================================================================================

EXECUTIVE SUMMARY
-----------------
Your LLM training has 3 CRITICAL configuration errors causing unstable loss:

  Issue             Your Config    Should Be    Impact
  ---------         -----------    ---------    ------
  Batch Size        5              12           High variance
  Learning Rate     5e-5           4.2e-4       8.4x TOO LOW
  Precision         fp16           bf16         Numerical instability

CONFIG COMPARISON
-----------------
Config                Batch  GradAcc  Effective         LR  Precision
---------------------------------------------------------------------------
❌ minimal (YOURS)        5        4         20   5.00e-05       fp16
✅ minimal_FIXED          12        1         12   4.20e-04       bf16
✅ small (PROVEN)         12        1         12   4.20e-04       bf16
✅ tiny (PROVEN)          16        1         16   4.20e-04       bf16

KEY FINDING: Your learning rate is 8.4x too low - this is the main cause!

ROOT CAUSES
-----------
1. ❌ Batch size 5 is too small for MoE (need ≥12)
   → High per-batch gradient variance
   
2. ❌ Learning rate 5e-5 is 8.4x too low (should be 4.2e-4)
   → Model barely learns, loss wanders randomly
   
3. ❌ Using fp16 instead of bf16
   → Numerical instability in MoE expert routing
   
4. ⚠️  Gradient clipping too aggressive (5.0→3.0)
   → Masks instability instead of fixing it
   
5. ⚠️  Adaptive LR enabled
   → Detects normal variance as problems, causes oscillations

SOLUTION (CHOOSE ONE)
---------------------

⭐ OPTION 1 (RECOMMENDED - 30 seconds):
   cp /project/minimal_config_FIXED.yaml /project/minimal_config.yaml
   cd /project/code/scripts/training
   python train.py --config /project/minimal_config.yaml

OPTION 2 (PROVEN STABLE):
   cd /project/code/scripts/training
   python train.py --config ../../configs/gpu/small.yaml

OPTION 3 (MANUAL FIX):
   Edit /project/minimal_config.yaml:
   - batch_size: 5 → 12
   - learning_rate: 5e-5 → 0.00042
   - mixed_precision: true → bf16
   - gradient_accumulation_steps: 4 → 1
   - Add: use_adaptive_lr: false

EXPECTED RESULTS
----------------
Before (Unstable):
  Epoch 1: Loss 8.2 → 12.4 → 6.1 → 15.2 → 9.8
  Epoch 2: Loss 11.3 → 7.5 → 18.1 → 5.9 → 14.2
  ❌ Oscillating wildly, not converging

After (Stable):
  Epoch 1: Loss 8.2 → 7.8 → 7.4 → 7.1 → 6.9
  Epoch 2: Loss 6.7 → 6.5 → 6.3 → 6.2 → 6.0
  ✅ Smooth, consistent decrease

QUICK START
-----------
Just copy-paste this command:

  cp /project/minimal_config_FIXED.yaml /project/minimal_config.yaml && \
  cd /project/code/scripts/training && \
  python train.py --config /project/minimal_config.yaml

Your loss will be stable within 1 epoch! 🎉

================================================================================
