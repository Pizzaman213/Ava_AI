╔══════════════════════════════════════════════════════════════════╗
║                   🚨 TRAINING FIX - QUICK REFERENCE               ║
╚══════════════════════════════════════════════════════════════════╝

PROBLEM: Model generates gibberish after 100k steps
ROOT CAUSE: Learning rate 0.0001 is 60x too low for pre-training

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ SOLUTION (Already Applied to Config)

  Learning Rate:  0.0001 → 0.006  (60x increase)
  Batch Size:     4 → 8           (2x increase)
  Effective Batch: 16 → 32        (smoother gradients)
  Grad Clip:      10.0 → 1.0      (tighter control)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 START TRAINING NOW

  cd /project/code/scripts/training
  
  python train.py \
    --config ../../configs/gpu/small.yaml \
    --fresh-start

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ VERIFY AFTER 1000 STEPS

  # Check loss dropped
  grep "step 1000" ../../outputs/runs/*/logs/training.log
  
  Expected: loss < 7.0 (ideally 6.0-6.5)
  Bad:      loss ~10.0 (no learning - increase LR more)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 EXPECTED PROGRESS

  Step 0:       loss = 10.5  (random)
  Step 1,000:   loss = 6.5   (learning!)
  Step 5,000:   loss = 4.2   (basic sentences)
  Step 10,000:  loss = 3.5   (coherent text)
  Step 100,000: loss = 2.5   (high quality)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

  Full Guide:    /project/TRAINING_FIXES.md
  Changes:       /project/CONFIG_CHANGES_SUMMARY.md
  Diagnostics:   /project/code/scripts/diagnose_training.py
  Config:        /project/code/configs/gpu/small.yaml

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 EMERGENCY FIXES

  If loss explodes (NaN):
    --learning-rate 0.003 --max-gradient-norm 0.5

  If OOM (Out of Memory):
    --batch-size 4 --gradient-accumulation 8

  If still gibberish at 10k steps:
    Keep training to 50k+ steps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Status: ✅ Config fixed | Ready to train
